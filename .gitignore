# Separate repos managed independently, not part of this one
/CoachWebapp/
/vendor/

# Xcode / Swift
.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
# `xcodebuild -derivedDataPath build` when installing on the phone — 165 MB
# of build products, not source.
/MoveLoad/build/
.build/
.swiftpm/
*.xcworkspace/xcuserdata/
Movesense_dfu*.zip
Movesense_combined.hex
_test_build/
_moveload_build/

# Recording data dropped in the root for offline analysis — the app's own
# session exports, third-party boat-sensor files, coaches' workbooks. Tens of
# megabytes each, athlete data, and not source. Matched by extension rather
# than by name: three rounds of adding one filename at a time was enough.
/*.csv
/*.sbem
/*.xlsm
/*.xlsx

# Build products, kept beside the project for Transporter.
/*.ipa
/preview-screenshot.png

# Firmware build folders created by Firmware/build.sh
/vendor/movesense-device-lib/_build_*
